let Router,postsCtrl,checkLoggedIn;_609‍.x([["default",()=>_609‍.o]]);_609‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_609‍.w("./posts.ctrl",[["*",null,function(v){postsCtrl=v}]]);_609‍.w("../../lib/checkLoggedIn",[["default",["checkLoggedIn"],function(v){checkLoggedIn=v}]]);

// 로그인 체크 미들웨어 사용(글작성, 글수정, 글삭제시 체크)


const posts = new Router();


// 각 주소가 호출되면 해당 함수 호출 
posts.get('/', postsCtrl.list);
posts.post('/', checkLoggedIn, postsCtrl.write);
// id 검증 함수 추가 
posts.get('/:id', postsCtrl.checkObjectId, postsCtrl.read);
posts.delete('/:id', checkLoggedIn, postsCtrl.checkObjectId, postsCtrl.remove);
posts.patch('/:id', checkLoggedIn, postsCtrl.checkObjectId, postsCtrl.update);
// posts.put('/:id', postsCtrl.replace);

// /api/posts/:id 용 라우터를 따로 만들어서 사용하는 방식 
// const post = new Router();
// post.get('/:id', postsCtrl.read);
// post.delete('/:id', postsCtrl.remove);
// post.patch('/:id', postsCtrl.update);

// posts.use('/:id', postsCtrl.checkObjectId, post.routes());

_609‍.d(posts);